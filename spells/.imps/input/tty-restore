#!/bin/sh
# tty-restore STATE - restore previously saved terminal settings
# Example: state=$(tty-save) && ... && tty-restore "$state"
set -eu


tr_state=${1:-}

if [ -z "$tr_state" ]; then
  printf '%s\n' "tty-restore: state required" >&2
  exit 1
fi

stty "$tr_state" >/dev/null 2>&1 || :
