#!/bin/sh
# strict - enable shell strict mode (set -eu)
# Testing imp - used in test scripts for strict error handling
# Example: . strict (sourced, not executed)

_test_strict() {
  set -eu
}

# Self-execute when run directly (not sourced)
case "$0" in
  */test-strict) _test_strict "$@" ;; esac
