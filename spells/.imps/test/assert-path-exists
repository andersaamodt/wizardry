#!/bin/sh
# assert-path-exists PATH - assert path exists
# Exits 0 if PATH exists, otherwise exits 1.
# Example: test-assert-path-exists "/tmp/file" || return 1

set -eu
if [ ! -e "$1" ]; then
  printf 'expected path to exist: %s\n' "$1" >&2
  exit 1
fi
