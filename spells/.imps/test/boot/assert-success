#!/bin/sh
# assert-success - verify STATUS is 0
# Shorthand for assert-status 0.
set -eu

_assert_success() {
  _assert_status 0
}

# Self-execution when run directly (not sourced)
set -eu
case "${0##*/}" in assert-success) _assert_success "$@" ;; esac
