#!/bin/sh
# assert-path-exists PATH - verify path exists
# Sets TEST_FAILURE_REASON if path does not exist.
set -eu

if [ ! -e "$1" ]; then
  TEST_FAILURE_REASON="expected path to exist: $1"
  exit 1
fi
exit 0


