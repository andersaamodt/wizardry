#!/bin/sh
# stub-systemctl DIR - create systemctl stub that logs args
set -eu

dir=$1
cat >"$dir/systemctl" <<'EOF_INNER'
#!/bin/sh
echo "status-ok"; printf '%s' "$*" >"$(dirname "$0")/systemctl.args"
EOF_INNER
chmod +x "$dir/systemctl"
