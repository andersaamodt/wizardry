#!/bin/sh
# write-command-stub DIR NAME - create a simple stub command that exits 0
# Creates an executable script at DIR/NAME that always succeeds.
set -eu

dir=$1
name=$2
cat <<'STUB' >"$dir/$name"
#!/bin/sh
exit 0
STUB
chmod +x "$dir/$name"


