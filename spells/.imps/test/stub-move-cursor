#!/bin/sh
# stub-move-cursor - test stub for move-cursor (no-op)
# Example: stub-move-cursor 10 20

set -eu

stub_move_cursor() {

    return 0
}

# Self-execute when run directly (not sourced)
case "$0" in
  */stub-move-cursor) stub_move_cursor "$@" ;; esac