#!/bin/sh
# stub-cursor-blink - test stub for cursor-blink that outputs escape codes
# Example: stub-cursor-blink on

set -eu

stub_cursor_blink() {

    case "${1-}" in
      on) printf '\033[?25h' ;;
      off) printf '\033[?25l' ;;
      *) return 0 ;;
    esac
}

stub_cursor_blink "$@"
