#!/bin/sh
# stub-cursor-blink - test stub for cursor-blink that outputs escape codes
# Example: stub-cursor-blink on

set -eu

  case "${1-}" in
    on) printf '\033[?25h' ;;
    off) printf '\033[?25l' ;;
    *) exit 0 ;;
  esac
