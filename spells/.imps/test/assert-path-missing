#!/bin/sh
# assert-path-missing PATH - assert path does not exist
# Exits 0 if PATH does not exist, otherwise exits 1.
# Example: test-assert-path-missing "/tmp/file" || return 1

set -eu
if [ -e "$1" ]; then
  printf 'expected path to be absent: %s\n' "$1" >&2
  exit 1
fi
