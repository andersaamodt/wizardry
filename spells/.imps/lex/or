#!/bin/sh
# or CMD... - execute remaining arguments as a command (alternation linking word)
# Used by parse-imperative for fallback: parse-imperative A or B → A || parse-imperative B
# Example: or echo fallback → echo fallback

set -eu

if [ $# -eq 0 ]; then
  exit 0
fi

exec "$@"
