#!/bin/sh
# then CMD CMD_ARGS MORE... - execute prior command, then continue if it succeeded
# Receives: $1=command, $2=command_args (space-separated), $3+=remaining args
# Example: parse echo hello then echo world → outputs "hello" then "world"

set -eu

cmd=${1:-}
cmd_args=${2:-}
shift 2 2>/dev/null || shift $#

# Run the prior command if present
if [ -n "$cmd" ]; then
  # shellcheck disable=SC2086
  if "$cmd" $cmd_args; then
    : # success
  else
    exit $?
  fi
fi

# Continue parsing remaining args
if [ $# -eq 0 ]; then
  exit 0
fi

exec parse "$@"
