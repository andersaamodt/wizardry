#!/bin/sh
# fail MESSAGE - print message to stderr and return failure (1)
# Example: has git || fail "git required"
set -eu

fail() {
  printf '%s\n' "$*" >&2
  return 1
}

# Self-execute when run directly (not sourced)
case "$0" in
  */fail) fail "$@"; exit 1 ;; esac
