#!/bin/sh
# info MESSAGE - print informational message with visual indicator
# Example: info "Processing files..."

set -eu

info_level=${WIZARDRY_LOG_LEVEL:-0}
if [ "$info_level" -ge 1 ]; then
  printf '%s\n' "$*"
fi
