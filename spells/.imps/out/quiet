#!/bin/sh
# quiet COMMAND [ARGS...] - run command with all output suppressed
# Example: quiet rm -f /tmp/file
set -eu

_quiet() {
  "$@" >/dev/null 2>&1
}

# Self-execute when run directly (not sourced)
case "$0" in
  */quiet) _quiet "$@" ;; esac
