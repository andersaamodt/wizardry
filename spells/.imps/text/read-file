#!/bin/sh
# read-file FILE - read entire file contents to stdout
# Example: content=$(read-file config.txt)

set -eu

_read_file() {
  cat "$1"
}

# Self-execute when run directly (not sourced)
case "$0" in
  */read-file) _read_file "$@" ;; esac
