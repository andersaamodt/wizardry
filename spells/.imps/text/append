#!/bin/sh
# append FILE - append stdin to file
# Example: echo "new line" | append log.txt
set -eu

_append() {
  cat >> "$1"
}

# Self-execute when run directly (not sourced)
case "$0" in
  */append) [ "${_WIZARDRY_SOURCING:-}" != "1" ] && _append "$@" ;; esac
