#!/bin/sh
# possessive NOUN - render NOUN in possessive form with appropriate apostrophe

set -eu

noun=${1-}
if [ -z "$noun" ]; then
        exit 1
fi
case $noun in
        *"'"|*"’") printf '%s' "$noun" ;;
        *[sS]) printf "%s'" "$noun" ;;
        *) printf "%s's" "$noun" ;;
esac

