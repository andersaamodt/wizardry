#!/bin/sh
# where COMMAND - print path to command if it exists, fail otherwise
# Example: git_path=$(where git)
set -eu

_where() {
  command -v "$1" 2>/dev/null || return 1
}

# Self-execute when run directly (not sourced)
set -eu
case "$0" in
  */where) _where "$@" ;; esac
