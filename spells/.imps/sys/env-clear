#!/bin/sh
# env-clear - marks spell as following no-env-var-passing policy
# This spell does not rely on environment variables for inter-script communication.
# Call near top of each spell after opening comment: . env-clear

# This is a marker/documentation imp. It serves to explicitly declare that
# the spell follows the no-env-var-passing policy. The actual enforcement
# is done by test_no_allcaps_variable_assignments which prevents ALL_CAPS
# local variables that could be confused with environment variables.

_env_clear() {
  # No-op: The test infrastructure enforces proper variable naming.
  # This marker documents that the spell follows the policy.
  return 0
}

# Self-execute when run directly (not sourced)
case "$0" in
  */env-clear) _env_clear "$@" ;; esac
