#!/bin/sh
# file-name PATH - extract filename from path (like basename but pure shell)
# Example: name=$(file-name /path/to/file.txt)  # outputs: file.txt

_file_name() {
  printf '%s\n' "${1##*/}"
}
