#!/bin/sh
# here - output current directory path (normalized pwd -P)
# Example: dir=$(here)
set -eu

_here() {
  pwd -P | sed 's|///*|/|g'
}

# Self-execute when run directly (not sourced)
case "$0" in
  */here) [ "${_WIZARDRY_SOURCING:-}" != "1" ] && _here "$@" ;; esac
