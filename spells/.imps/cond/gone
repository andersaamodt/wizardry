#!/bin/sh
# gone PATH - test if path does NOT exist
# Example: gone /etc/config && echo "not found"

_gone() {
  [ ! -e "$1" ]
}

# Self-execute when run directly (not sourced)
case "$0" in
  */gone) [ "${_WIZARDRY_SOURCING:-}" != "1" ] && _gone "$@" ;; esac
