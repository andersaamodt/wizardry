#!/bin/sh
# matches PATTERN FILE - test if file contains regex pattern
# Example: matches "^ID=nixos" /etc/os-release && echo "NixOS detected"

_matches() {
  grep -q -- "$1" "$2" 2>/dev/null
}

# Self-execute when run directly (not sourced)
case "$0" in
  */matches) _matches "$@" ;; esac
