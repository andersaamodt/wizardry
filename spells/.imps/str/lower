#!/bin/sh
# lower - convert stdin to lowercase
# Example: echo "HELLO" | lower
set -eu

_lower() {
  tr '[:upper:]' '[:lower:]'
}

# Self-execute when run directly (not sourced)
case "$0" in
  */lower) _lower "$@" ;; esac
