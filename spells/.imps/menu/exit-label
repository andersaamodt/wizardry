#!/bin/sh
# exit-label - return the label for menu exit items
# Example: exit_item="$(exit-label)%kill -TERM \$PPID"

exit_label() {
  printf '%s' "Exit"
}

# Self-execute when run directly (not sourced)
case "$0" in
  */exit-label) exit_label "$@" ;; esac
