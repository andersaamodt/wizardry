#!/usr/bin/env sh
# This spell installs the xsel clipboard utility using the system package manager.
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname "$0")" && pwd -P)
ASSUME_YES=1 "$SCRIPT_DIR/manage-system-command" xsel xsel
