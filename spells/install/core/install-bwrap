#!/usr/bin/env sh

# Install the bubblewrap sandboxing utility across common platforms.

set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname "$0")" && pwd -P)
ASSUME_YES=1 FORCE_INSTALL=${INSTALL_BWRAP_FORCE_INSTALL:-0} "$SCRIPT_DIR/manage-system-command" bwrap bubblewrap
