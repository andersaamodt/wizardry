#!/usr/bin/env sh
# This spell uninstalls the xclip clipboard utility.

show_usage() {
  cat <<'USAGE'
Usage: uninstall-xclip

Uninstall the xclip clipboard utility.
USAGE
}

case "${1-}" in
--help|--usage|-h)
  show_usage
  exit 0
  ;;
esac

set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname "$0")" && pwd -P)
"$SCRIPT_DIR/manage-system-command" --uninstall xclip xclip
