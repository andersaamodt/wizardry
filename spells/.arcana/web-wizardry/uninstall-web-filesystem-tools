#!/bin/sh

case "${1-}" in
--help|--usage|-h)
  cat <<'USAGE'
Usage: uninstall-filesystem-tools

Removes filesystem growth tools used by Wizardry and Headquarters.
USAGE
  exit 0
  ;;
esac

require-wizardry || exit 1

exec uninstall-filesystem-tools "$@"
