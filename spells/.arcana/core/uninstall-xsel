#!/bin/sh
# Bootstrap spell: uninstalls the xsel clipboard utility.

uninstall_xsel_usage() {
        cat <<'USAGE' >&2
Usage: uninstall-xsel

This spell uninstalls the xsel clipboard utility.
USAGE
}

uninstall_xsel() {
case "${1-}" in
--help|--usage|-h)
        uninstall_xsel_usage
        return 0
        ;;
esac

require-wizardry || return 1


set -eu
. env-clear

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname "$0")" && pwd -P)
"$SCRIPT_DIR/manage-system-command" --uninstall xsel xsel

}

# Load castable imp for direct execution
if true; then  # Always source castable, never use from PATH
  _d=$(CDPATH= cd -- "$(dirname "$0")" && pwd -P)
  _r=$(cd "$_d" && while [ ! -d "spells/.imps" ] && [ "$(pwd)" != "/" ]; do cd ..; done; pwd)
  _i="${WIZARDRY_DIR:-${_r}}/spells/.imps/sys"
  [ -f "$_i/castable" ] && . "$_i/castable"
fi

castable "$@"