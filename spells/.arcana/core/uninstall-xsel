#!/bin/sh

# Bootstrap spell: Uninstall xsel (X11 clipboard utility) using the system package manager.

case "${1-}" in
--help|--usage|-h)
  cat <<'USAGE'
Usage: uninstall-xsel

This spell uninstalls the xsel clipboard utility.
USAGE
  exit 0
  ;;
esac

set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname "$0")" && pwd -P)
"$SCRIPT_DIR/manage-system-command" --uninstall xsel xsel
