#!/bin/sh
# Bootstrap spell: uninstalls the dd command using the system package manager.

show_usage() {
        cat <<'USAGE' >&2
Usage: uninstall-dd

This spell uninstalls the dd command using the system package manager.
USAGE
}

case "${1-}" in
--help|--usage|-h)
        show_usage
        exit 0
        ;;
esac

set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname "$0")" && pwd -P)

APT_PACKAGE=coreutils
DNF_PACKAGE=coreutils
YUM_PACKAGE=coreutils
ZYPPER_PACKAGE=coreutils
PACMAN_PACKAGE=coreutils
APK_PACKAGE=coreutils
BREW_PACKAGE=coreutils
ASSUME_YES=1

export APT_PACKAGE DNF_PACKAGE YUM_PACKAGE ZYPPER_PACKAGE
export PACMAN_PACKAGE APK_PACKAGE BREW_PACKAGE ASSUME_YES

"$SCRIPT_DIR/manage-system-command" --uninstall dd coreutils
