#!/bin/sh

# This spell displays the ao-mud main menu.


set -eu
if [ -n "${REQUIRE_COMMAND-}" ]; then
  require_cmd=$REQUIRE_COMMAND
else
  require_cmd=require-command
fi

# Shared helper for dependency checks.
require() {
  "$require_cmd" "$@"
}

load_colors() {
  if has colors; then
    # shellcheck source=/dev/null
    . "$(command -v colors)"
  else
    # Proceed without colour accents when the palette is missing.
    RESET=''
    CYAN=''
    GREY=''
  fi
}

load_colors

if ! require menu "The main menu needs the 'menu' command to present options."; then
  exit 1
fi

display_menu() {
  mud="MUD menu%WIZARDRY_SUBMENU=1 mud"
  cast="Cast a Spell%WIZARDRY_SUBMENU=1 cast"
  spellbook="Spellbook%WIZARDRY_SUBMENU=1 spellbook"
  install="Install Free Software%WIZARDRY_SUBMENU=1 install-menu"
  system="Manage System%WIZARDRY_SUBMENU=1 system-menu"
  exit_label=$(exit-label)
  exit_item="${exit_label}%kill -2 $$"

  set -- "$mud" "$cast" "$spellbook" "$install" "$system" "$exit_item"
  MENU_ESCAPE_STATUS=$menu_escape_status menu "Main Menu:" "$@"
}

# Catch Ctrl-C and exit
handle_ctrl_c() {
  trap 'exit' INT
}

handle_ctrl_c

menu_escape_status=113

first_run=1

while true; do
  if [ "$first_run" -eq 0 ]; then
    printf '\n'
  else
    first_run=0
  fi

  if display_menu; then
    status=0
  else
    status=$?
  fi

  if [ "$status" -eq "$menu_escape_status" ]; then
    break
  fi

  if [ "$status" -ne 0 ]; then
    exit "$status"
  fi
done
