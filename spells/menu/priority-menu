#!/bin/sh

# This spell displays a menu of commands for a prioritized file or folder.
# Use 'priorities' to easily select a priority with this menu.

show_usage() {
        cat <<'USAGE'
Usage: priority-menu <file>

Display an interactive menu of actions for a prioritized file, including
options to prioritize, check, edit, or browse within the file.
USAGE
}

case "${1-}" in
--help|--usage|-h)
        show_usage
        exit 0
        ;;
esac

set -eu

. colors

FILE="${1-}"

if [ -z "$FILE" ]; then
        printf '%s\n' "priority-menu: file argument required." >&2
        exit 1
fi

display_menu() {
	checked_state=$(read-magic "${FILE}" checked)
	if [ "${checked_state}" = "Error: The attribute does not exist." ]; then
	  check_item="Check%echo 'Placeholder: Check command'"
	else
	  check_item="Uncheck%echo 'Placeholder: Uncheck command'"
	fi
	
	filename=$(basename "$FILE")
	
  menu "${GREEN}${BOLD}${filename}${RESET}" \
       "Prioritize (upboat)%prioritize \"$FILE\"; kill -2 $$" \
       "${check_item}" \
       "Discard from priorities (downboat)%echo 'Placeholder: Discard command'" \
       "Edit Card%echo 'Placeholder: Edit command'" \
       "Browse Within%echo 'Placeholder: Browse files within this file/folder'" \
       "Exit%kill -2 $$"
}

trap 'exit' INT

while true; do
  display_menu
done