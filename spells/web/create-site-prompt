#!/bin/sh

# Prompt for site name and create it.


set -eu
. env-clear

# Prompt for site name
printf 'Enter site name: '
read -r site_name

if [ -z "$site_name" ]; then
  warn "create-site-prompt: no site name entered"
  exit 1
fi

create-site "$site_name"
