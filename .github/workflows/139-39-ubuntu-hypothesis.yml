name: 139-39 Ubuntu disable gloss cache regen

on:
  pull_request:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  ubuntu-hypothesis-39:
    name: 139-39 Ubuntu disable gloss cache regen
    runs-on: ubuntu-latest
    env:
      WIZARDRY_OS_LABEL: ubuntu
      WIZARDRY_GLOSSES: "1"
      WIZARDRY_DEBUG: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Ensure installer spells are executable
        run: chmod +x spells/.arcana/mud/install-mud spells/.arcana/tor/setup-tor spells/.imps/fs/xattr-helper-usable spells/.imps/fs/xattr-list-keys spells/.imps/fs/xattr-read-value
      - name: Install bubblewrap dependencies
        run: sudo apt-get update && sudo apt-get install -y bubblewrap uidmap attr file
      - name: Configure bubblewrap user namespaces
        run: |
          sudo usermod --add-subuids 100000-165535 --add-subgids 100000-165535 "$(whoami)"
          sudo usermod --add-subuids 100000-165535 --add-subgids 100000-165535 root
          {
            echo 'kernel.unprivileged_userns_clone=1'
            echo 'kernel.apparmor_restrict_unprivileged_userns=0'
          } | sudo tee /etc/sysctl.d/99-unprivileged-userns.conf
          sudo sysctl --system
      - name: Patch wizardry scripts for hypothesis
        run: |
          python - <<'PY'
from pathlib import Path
path = Path('spells/.imps/sys/invoke-wizardry')
text = path.read_text()
text = text.replace('if [ "$_iw_needs_regen" -eq 1 ]; then', 'if false; then # disabled regen')
path.write_text(text)
PY
      - name: Run subshell banish repro
        run: |
          set +e
          ulimit -c unlimited || true
          if command -v timeout >/dev/null 2>&1; then
            timeout 120 bash -lc '. spells/.imps/sys/invoke-wizardry && banish 8 --only --no-tests --no-heal'
            status=$?
          else
            bash -lc '. spells/.imps/sys/invoke-wizardry && banish 8 --only --no-tests --no-heal'
            status=$?
          fi
          if [ "$status" -eq 139 ]; then
            printf '%s
' "subshell banish exited with code 139 (SIGSEGV)"
            printf '%s
' "Last 50 dmesg lines (if available):"
            dmesg | tail -n 50 || true
          elif [ "$status" -eq 124 ]; then
            printf '%s
' "subshell banish timed out after 120 seconds"
          fi
          exit "$status"
