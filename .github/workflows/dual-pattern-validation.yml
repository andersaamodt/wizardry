name: Dual-pattern validation (DEPRECATED)

# DEPRECATED: This workflow is no longer needed.
# Spells are now flat, linear scripts without function wrappers.
# The castable/uncastable pattern with dual-pattern testing is deprecated.
# See .github/instructions/castable-uncastable-pattern.instructions.md for details.
#
# This workflow is kept for historical reference but does not run.

on:
  # Disabled - no triggers
  workflow_dispatch:
    inputs:
      deprecated:
        description: 'This workflow is deprecated and should not be run'
        required: false
        default: 'true'

permissions:
  contents: read

jobs:
  deprecated-notice:
    name: Deprecated workflow
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "⚠️ This workflow is deprecated."
          echo "Spells are now flat, linear scripts."
          echo "Dual-pattern testing is no longer applicable."
          exit 1
